<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin">
		
	<!-- 환자목록 조회 -->
	<select id="patient_list" resultType="admin.vo.PatientVO">
		SELECT p.*
		FROM patient p
		INNER JOIN medical_receipt r ON p.patient_id = r.patient_id 
		WHERE r.time BETWEEN current_date AND current_date + 1
		ORDER BY p.patient_id
	</select>


</mapper>